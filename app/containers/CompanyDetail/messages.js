/*
 * CompanyDetail Messages
 *
 * This contains all the text for the CompanyDetail container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CompanyDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CompanyDetail container!',
  },
});
