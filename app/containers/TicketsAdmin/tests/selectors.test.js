// import { selectTicketsAdminDomain } from '../selectors';

describe('selectTicketsAdminDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
