/*
 * SolutionDetail Messages
 *
 * This contains all the text for the SolutionDetail container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SolutionDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SolutionDetail container!',
  },
});
