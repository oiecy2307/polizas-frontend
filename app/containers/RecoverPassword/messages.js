/*
 * RecoverPassword Messages
 *
 * This contains all the text for the RecoverPassword container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RecoverPassword';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RecoverPassword container!',
  },
});
