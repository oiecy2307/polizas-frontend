/*
 * Invitations Messages
 *
 * This contains all the text for the Invitations container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Invitations';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Invitations container!',
  },
});
