// import { selectDashboardBackofficeDomain } from '../selectors';

describe('selectDashboardBackofficeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
