/*
 * TicketsReporter Messages
 *
 * This contains all the text for the TicketsReporter container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TicketsReporter';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TicketsReporter container!',
  },
});
