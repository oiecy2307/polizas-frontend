/*
 * NewInvitationDialog Messages
 *
 * This contains all the text for the NewInvitationDialog component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.NewInvitationDialog';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NewInvitationDialog component!',
  },
});
