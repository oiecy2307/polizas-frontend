/*
 * AssignTicketDialog Messages
 *
 * This contains all the text for the AssignTicketDialog component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AssignTicketDialog';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AssignTicketDialog component!',
  },
});
