/*
 * PayTicketDialog Messages
 *
 * This contains all the text for the PayTicketDialog component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.PayTicketDialog';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PayTicketDialog component!',
  },
});
